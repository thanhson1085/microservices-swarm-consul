description "Consul Server"
 
start on runlevel [2345]
stop on runlevel [!2345]
 
setuid root
setgid root

exec consul agent -server -bootstrap-expect 1 \
	-data-dir /tmp/consul -node=gateway -bind=0.0.0.0 \
	-client=0.0.0.0 \
	-config-dir /etc/consul.d -ui-dir /opt/consul/
